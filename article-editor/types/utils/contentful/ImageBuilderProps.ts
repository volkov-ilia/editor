import { Upload } from "contentful-management/types"

type ImageBuilderProps = {
  fileName: string
  assetName: string
  upload: Upload
  fileType: string
  description: string
}

export default ImageBuilderProps
